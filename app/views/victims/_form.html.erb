<%= form_with(model: victim, local: true) do |form| %>
  <% if victim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(victim.errors.count, "error") %> prohibited this victim from being saved:</h2>

      <ul>
      <% victim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cin %>
    <%= form.text_field :cin, id: :victim_cin %>
  </div>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :victim_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :victim_last_name %>
  </div>

  <div class="field">
    <%= form.label :problem %>
    <%= form.text_field :problem, id: :victim_problem %>
  </div>

  <div class="field">
    <%= form.label :cause_of_death %>
    <%= form.text_field :cause_of_death, id: :victim_cause_of_death %>
  </div>

  <div class="field">
    <%= form.label :date_of_death %>
    <%= form.date_select :date_of_death, id: :victim_date_of_death %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
